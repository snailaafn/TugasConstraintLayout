<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Warna latar belakang -->
    <corners android:radius="12dp" /> <!-- Radius untuk sudut membulat -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- Garis tepi -->
</shape>
